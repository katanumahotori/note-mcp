# note-mcp で作業する AI へ

note.com の記事を AI から作成・編集・公開するための MCP サーバー。[drillan/note-mcp](https://github.com/drillan/note-mcp) のフォークで、片沼さんの Claude Code と Codex はこのフォルダを直接起動して使う。
記事を書く作業そのものは HP／情報発信の仕事場で行う。ここを開くのは、note の操作でツール側の不具合や機能不足に当たったときだけ。

## 最初に読む

- `.handoff/` に未処理の依頼があれば最初に読み、それを今回の仕事にする（処理後は `.handoff/done/` へ）
- [README の「このフォークについて」](README.md#このフォークについて)：上流との差分、どこから起動されるか、触ってよい範囲。
- [docs/quickstart.md](docs/quickstart.md)：MCP クライアントの設定と基本の使い方。
- [.specify/memory/constitution.md](.specify/memory/constitution.md)：上流の開発規約（TDD・型注釈・DRY・命名）。このリポジトリで書くコードはこれに従う。
- [DISCLAIMER.md](DISCLAIMER.md)：note.com の非公式 API を使っていること。

## 作業の流れ

1. ブランチと未コミット差分を確認する。作業ブランチは `local/combined`、上流は remote `origin`、片沼さんのフォークは remote `fork`。
2. 症状が本当にここの問題かを先に切り分ける。多くは呼び出し側（記事の Markdown や渡し方）の問題で、その場合ここは触らない。
3. 直すと決めたら、同じ修正が上流に入っていないかを `git log origin/main` と上流の issue で確認する。入っていれば取り込むだけで済む。
4. 先に落ちるテストを書いてから実装する（上流規約の Article 1）。型注釈を付け、`Any` で逃げない（Article 9）。
5. 下の検証を通す。
6. 完了した論理単位を親が明示ファイル指定でコミットする。

## 検証を選ぶ

```bash
uv sync --group dev
uv run pytest
uv run ruff check --fix . && uv run ruff format . && uv run mypy .
```

| 変更 | 確認 |
|---|---|
| 文書のみ | リンクと実装との整合 |
| 変換・埋め込みなど純粋なロジック | 該当のユニットテスト＋lint・format・mypy |
| note.com の API を叩く経路 | 上記全部＋実際に MCP 経由で下書きを1本作って確認。**コードを変えたら Claude Code / Codex を再起動しないと反映されない**（stdio サーバーはクライアント起動時に読み込まれるため） |

E2E（`tests/e2e/`）は既定で headless。ブラウザを見たいときだけ `NOTE_MCP_TEST_HEADLESS=false`。

## 守る境界

- note.com への**公開は外部公開**。`note_publish_article` は片沼さんの承認なしに実行しない。下書きの作成・更新までは確認不要。
- 動作確認で実アカウントに下書きを撒かない。作った下書きは確認後に消す。
- テストや例で使う URL は実在するものだけにし、架空の URL を生成しない（上流規約 Article 6）。既存テストが使っている URL をそのまま流用する。
- 認証は Playwright での手動ログインと OS のキーチェーン保存。セッション情報をファイルやログへ書き出さない。
- ルビは `｜漢字《かんじ》` 記法をそのまま送る。`<ruby>` タグを直接送るとサニタイズされる。太字とルビの併用は効かない（上流 Issue #169）。
- 非公式 API なので、挙動が分からないときは推測で実装せず、実際のレスポンスを確認してから決める。
- 上流へ戻す気のない改造を増やさない。差分が増えるほど上流の更新を取り込めなくなる。

## 使うスキル

- delegation：子 AI へ渡すとき。所有ファイル・禁止範囲・検証方法を渡し、git 統合は親が行う。

## 完了報告

「変えたこと・検証できたこと・未確認・次に必要な判断」を短く。MCP の再起動が必要かどうかを必ず書く。毎回同じ操作になっている手順があれば「仕組みに昇格できる」と1行提案する。

承認済みの範囲は確認せず完遂する。止まるのは4類型（公開・課金・送信・削除）と指示書の停止条件だけ。
小さな変更にテストを書き足さない（振る舞いを変える変更の回帰テストは別で、これは必須）。
スキルが衝突したら名指しされた SKILL.md を優先し、片沼さんの直接の指示を最優先にする。子 AI に再委譲させない。
